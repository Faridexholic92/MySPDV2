'use client';import{FormEvent,useState}from'react';import{useRouter}from'next/navigation';import{configured,supabase}from'@/lib/supabase';export default function Login(){const r=useRouter(),[e,setE]=useState(''),[busy,setBusy]=useState(false);async function go(x:FormEvent<HTMLFormElement>){x.preventDefault();setBusy(true);const f=new FormData(x.currentTarget);if(!configured){r.push('/');return}const{error}=await supabase.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(error){setE('Email atau kata laluan tidak sah.');setBusy(false)}else r.push('/')}return <main className="login"><section><div className="brand"><b>M</b><span><strong>MYSPD</strong><small>Command Center</small></span></div><article><em>PORTAL DALAMAN SPD</em><h1>Satu ruang kerja.<br/>Semua operasi.</h1><p>Akses pantas kepada permohonan, status, dokumen, agenda dan komuniti Seksyen Penawanan Data.</p></article></section><form onSubmit={go}><b className="mobileBrand">MYSPD</b><em>SELAMAT DATANG</em><h2>Log masuk ke portal</h2><p>Gunakan akaun yang telah didaftarkan oleh pentadbir.</p>{e&&<aside>{e}</aside>}<label>Email<input name="email" type="email" placeholder="nama@jupem.gov.my" required/></label><label>Kata laluan<input name="password" type="password" placeholder="••••••••" required/></label><button disabled={busy}>{busy?'Menyambung…':'Log masuk →'}</button><small>Portal MySPD · Seksyen Penawanan Data</small></form></main>}
