import {createClient} from '@supabase/supabase-js';const u=process.env.NEXT_PUBLIC_SUPABASE_URL||'';const k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY||'';export const configured=!!(u&&k&&!k.startsWith('PASTE_'));export const supabase=createClient(u||'https://placeholder.supabase.co',configured?k:'placeholder');